# Package the Number/String Component slice / 打包 Number/String Component 首段

- Consume the public Component Interface IR v1 envelope without depending on
  Calcit core internals. Accept the default Cirru EDN representation and retain
  JSON as an explicit compatibility projection; validate schema identity,
  filters, summary, diagnostics, revision, direction, module, symbol, and typed
  signatures before generation.
- Reuse the existing `validate`, `generate`, and `check` commands. Component
  generation requires `--core-module`, selects the existing WIT backend, and
  creates no parallel top-level tool entry point.
- Render deterministic WIT for the strict monomorphic Number/String slice,
  preserve exact import module and import/export symbol identities, and reject
  names that would require a lossy rewrite. Correct the native WIT Number
  spelling from legacy `float64` to current `f64`.
- Embed the generated WIT metadata into the Calcit core module and use Bytecode
  Alliance `wit-component` to encode and validate a runnable WebAssembly
  Component before touching the managed output directory.
- Extend manifest schema v3 with the contract kind and core-module digest. Own
  the canonical contract projection, WIT world, and packaged Component so
  `check` deterministically detects stale contract, core, or generated bytes.
- Exercise direct Number/String exports and Number/String host imports through
  Wasmtime. Pin jco in CI, transpile the same generated Component, and execute
  the same four semantic paths in Node.js without a silent skip.
- Update the bilingual README and repository guidance to document the current
  ownership boundary, Cirru EDN default, explicit JSON compatibility, command
  examples, artifact set, strict limitations, and cross-host release evidence.
