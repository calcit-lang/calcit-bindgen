# 发布 0.1.8 / Release 0.1.8

合并 #54 并处理评审后，发布 WASI v0.3.0 `wasi:cli/command` Component 的固定 WIT 封装 API。该版本提供 Calcit #1266 所需的生产依赖；它不声称已经实现 Calcit 端的参数、环境或标准流 lowering。

After merging #54 and addressing review, release the pinned WASI v0.3.0 command Component packaging API. This supplies the production dependency for Calcit #1266; it does not claim Calcit-side argument, environment, or stdio lowering.
