# Clarify shared contract command help / 澄清共享契约命令帮助

- Describe `validate` as accepting both supported native and Component
  contracts instead of retaining the obsolete v2-only wording.
- Clarify that omitted backend selection means every backend for native
  contracts and the WIT packaging path for Component contracts.
- Keep `check` wording centered on the manifest-owned backend set shared with
  generation.
