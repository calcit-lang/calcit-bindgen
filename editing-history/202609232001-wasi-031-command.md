# WASI 0.3.1 command packaging / WASI 0.3.1 命令封装

2026-09-23 20:01 +0800

- 从 WebAssembly/WASI v0.3.1 正式 tag 合并 CLI、clocks、filesystem、sockets、random WIT，保留原始接口语义与 `@since`，只整理 package 声明和行尾空格；记录来源与 SHA-256。
- 将 `package_wasi_command` 迁移到 `wasi:cli/run@0.3.1`；旧版 0.3.0 core export 明确拒绝。
- 使用官方 Wasmtime 49.0.0 验证零导入命令及真实 `wasi:cli/exit@0.3.1` 宿主导入，CI 固定可执行文件与校验和。
- 跑通 `cargo fmt --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test` 与带 Wasmtime 49 的 command smoke；提交后运行 `cargo package`。

---

- Merge the official WebAssembly/WASI v0.3.1 CLI, clocks, filesystem, sockets, and random WIT by package, preserving interface semantics and `@since` while normalizing package declarations and trailing whitespace; record provenance and SHA-256.
- Move `package_wasi_command` to `wasi:cli/run@0.3.1` and reject the old 0.3.0 core export.
- Exercise a no-import command and a real `wasi:cli/exit@0.3.1` host import with the official Wasmtime 49.0.0 binary; pin the CI binary checksum.
- Pass `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`, and the Wasmtime 49 command smoke; run `cargo package` after commit.
